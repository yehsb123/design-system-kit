# 블로그 소스 브리프 — 디자인시스템 모음집 프로젝트

> 이 파일은 블로그 글을 쓰기 위한 **원자료(브리프)**입니다.
> 블로그 작성 세션에 이 내용을 붙여넣고 "이 자료로 블로그 글을 써줘"라고 요청하면 됩니다.
> 아래 사실(Fact)은 실제 산출물에서 확인된 내용이며, 과장 없이 그대로 씁니다.

---

## 0. 한 줄 요약

주요 디자인시스템 16개를 **하나의 토큰 구조로 재구성**하고, 색 램프만 넣으면 **라이트/다크를 자동 생성**하며, **CSS·Tailwind·JSON·React·AI 프롬프트로 통째 내보내는** 의존성 0의 단일 HTML 도구를 만들었다. 매 프로젝트를 0에서 시작하지 않기 위해서다.

---

## 1. 왜 만들었나 (문제의식)

- 웹/앱 프로젝트를 시작할 때마다 색·타이포·간격·컴포넌트 규칙을 매번 새로 정하는 데 시간이 든다.
- 시안(디자인)과 실제 코드가 어긋나는 **정합성 문제**가 반복된다.
- 라이트/다크를 나중에 대응하려다 색 체계가 무너진다.
- 참고할 만한 좋은 시스템(Toss, Material, Carbon 등)은 많지만, **서로 구조가 달라 비교·재사용이 어렵다.**

→ "좋은 시스템들을 같은 틀에 정리해두고, 필요할 때 골라 즉시 쓴다"는 창고(라이브러리)가 필요했다.

---

## 2. 핵심 개념 (블로그에서 가장 강조할 부분)

### 2-1. 램프(Ramp) → 시맨틱(Semantic) 자동 생성
- **램프**: 색의 50→950 단계(예: primary, gray, success, warning, error, info).
- 램프만 정의하면, 함수 하나(`makeSem`)가 **라이트/다크용 시맨틱 토큰을 자동으로 뽑아낸다.**
  - 예: 라이트는 `primary-500`을 버튼 배경으로, 다크는 `primary-400`을 배경으로 자동 매핑.
- 즉, **색 단계 한 벌만 만들면 → 라이트/다크 전체 화면 색 체계가 완성된다.**
- 이게 "다크모드 나중에 붙이다 무너지는" 문제를 구조적으로 제거한다.

### 2-2. 토큰 3계층 구조
1. **원시 램프** (테마 무관, 50~950 색 단계)
2. **시맨틱 토큰** (역할 기반: `--bg-primary`, `--fg-lower`, `--border`, `--bg-critical` 등 / 라이트·다크 분리)
3. **컴포넌트 토큰** (모서리·두께·그림자·패딩 등 시스템별 시그니처: `--btn-radius`, `--card-radius` 등)

→ 개발자가 하드코딩 색이 아니라 **역할 토큰**을 쓰게 되어, 브랜드 색만 바꾸면 전체가 갈아끼워진다.

### 2-3. "그대로 개발에 쓰는" 익스포트
카드 하나를 열면 그 시스템 전체를 아래 형식으로 내보낼 수 있다.
- **CSS 변수** (`:root { --primary-500: ... }`)
- **Tailwind 설정**
- **JSON 토큰**
- **React** 코드
- **AI 프롬프트** (Claude 등에 넣어 "이 토큰으로 만들어줘" 용)

→ 디자인 자료가 아니라 **바로 코드에 붙는 자산**이라는 점이 핵심.

---

## 3. 담긴 시스템 (2026년 8월 기준, 계속 확장 중)

| 시스템 | 성격 | 브랜드 색/특징 |
|---|---|---|
| Toss (TDS) | 앱·핀테크 | 토스 블루, 명료한 그레이 |
| Aurora | 범용 SaaS | 블루+틸, 태그 12색·차트 풀세트 |
| Graphite | 엔터프라이즈 | 인디고+슬레이트, 고밀도 그리드 |
| Ant Design | 엔터프라이즈 | Daybreak Blue #1677FF |
| Material 3 | 앱 | 보라 프라이머리, 부드러운 라운드 |
| IBM Carbon | 엔터프라이즈 | 각진 형태, 명확한 계층 |
| Apple HIG | 앱 | System Blue, 시스템 그레이 |
| Shopify Polaris | 커머스 | 그린 브랜드, 잉크 중립색 |
| Neon | 랜딩 | 다크 우선, 바이올릿→시안 글로우 |
| Editorial | 매거진 | 세리프, 높은 대비, 각진 형태 |
| Playful | 랜딩·캠페인 | 캔디 팔레트, 알약형 버튼 |
| Mobile Kit | 모바일 | 큰 터치 타깃, 민트 브랜드 |
| Vercel / Geist | 랜딩·개발 | 흑백 모노크롬 미니멀 |
| GitLab | 데브옵스 | 오렌지 브랜드 |
| Airbnb | 여행·숙박 | 코랄/로즈, 사진 중심 카드 |
| Slack | 협업 | 오버진(플럼)+시안 |

> 참고: 위 시스템들은 **공개된 각 브랜드의 디자인 언어를 같은 토큰 틀로 재구성한 학습·참고용**이다. 각 브랜드의 상표가 아니라, "이렇게 구조화하면 비교·재사용이 된다"는 방법론의 예시로 삼았다.

---

## 4. 기술적 특징 (개발 블로그면 강조)

- **단일 HTML 파일, 의존성 0** — 빌드·설치 없이 더블클릭으로 열린다.
- 새 시스템 추가 = 배열에 객체 하나 push (`mkSystem({...})`) → 나머지는 자동 생성.
- 라이트/다크 실시간 토글로 **양쪽 모드를 즉시 검증**.
- 업종별 추천 템플릿(병원·커머스·랜딩 등)으로 "무엇을 만들지 → 어떤 시스템으로 시작할지" 제안.

---

## 5. 실제로 어떻게 쓰는가 (활용 스토리 — 블로그의 결론부에 좋음)

- **속도**: 프로젝트마다 색·컴포넌트를 0에서 안 짜고, 시스템 하나 골라 브랜드 색만 교체 → 시작점이 즉시 나온다.
- **일관성**: 역할 토큰 기반이라 화면이 많아져도 규칙이 안 무너진다.
- **AI 협업**: 토큰을 AI 프롬프트로 내보내, "이 토큰만 써서 화면 만들어"라고 시키면 임의 색·컴포넌트 없이 규칙 안에서 조립된다.
- **자기 증명**: 이 도구로 만든 결과물(예: 개인 포트폴리오 사이트) 자체가 시스템의 라이브 데모가 된다.

---

## 6. 블로그 구성안 (제안 — 이 순서로 쓰면 됨)

1. **훅** — "프로젝트 시작할 때마다 색부터 다시 정하는 게 지겨웠다"
2. **문제** — 정합성·다크모드·재사용 (섹션 1)
3. **해결의 핵심 아이디어** — 램프 하나 → 라이트/다크 자동 (섹션 2-1)
4. **어떻게 구조화했나** — 토큰 3계층 (섹션 2-2)
5. **바로 쓰는 자산** — 익스포트 (섹션 2-3)
6. **무엇을 담았나** — 16개 시스템 표 (섹션 3)
7. **실제 효과** — 활용 스토리 (섹션 5)
8. **마무리** — 앞으로의 확장 방향(시스템 추가, 스타터 템플릿화 등)

---

## 7. 제목 후보

- 색 램프 하나로 라이트·다크를 자동 생성하는 디자인시스템 만들기
- 매번 0에서 시작하지 않기 위해, 디자인시스템 16개를 한 틀에 정리했다
- 디자인 토큰을 "코드에 바로 붙는 자산"으로 만드는 법
- 단일 HTML 파일로 만든 디자인시스템 라이브러리

---

## 8. 톤 가이드 (작성 시 지킬 것)

- 담백하게. 과장·마케팅 수식어("혁신적", "게임체인저") 금지.
- 같은 내용 반복 금지 — 각 섹션은 고유한 정보만.
- 코드/개념은 구체적으로. 추상적 미사여구보다 "램프 → 시맨틱" 같은 실제 메커니즘 위주.
- 스크린샷 삽입 위치 표시: [히어로 캡처], [라이트/다크 토글 비교], [익스포트 화면] 등.
